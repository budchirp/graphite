#include <iostream>

#include "utils/logger/logger.hpp"

// TODO: Add line info
void Logger::log(string message) { cout << message << endl; }
void Logger::warn(string message) { cout << message << endl; }
void Logger::error(string message) { cout << message << endl; }

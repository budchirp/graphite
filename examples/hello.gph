extern printf(str: string) -> void

fn main() -> i32 {
  printf("hi")

  var x: i32 = (32 + 32)
  var y: i32 = (16 * 4)

  var z: string = if (x == y) { true } else { false }

  if (true) {
    printf("x and y is 64")
  } else {
    printf("x and y is not 64")
  }

  printf("asd")

  return 1
}
